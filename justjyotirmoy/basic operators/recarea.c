//rectangle ka area

#include <stdio.h>

int main() 
{
    int length = 35;
    int width = 10;
    int area = length * width;

    printf("\ngiven numbers are: %d and %d\n", length, width);
    printf("Length: %d\n", length);
    printf("Width: %d\n", width);
    printf("Area: %d\n", area);

    return 0;
}
