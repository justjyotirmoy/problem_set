//rectangle ka perimeter

#include <stdio.h>

int main() 
{
    int length = 28;
    int width = 9;
    int perimeter = 2 * (length + width);

    printf("\ngiven numbers are: %d and %d\n", length, width);
    printf("Length: %d\n", length);
    printf("Width: %d\n", width);
    printf("Perimeter: %d\n", perimeter);

    return 0;
}
