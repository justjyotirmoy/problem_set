//triangle ka area

#include <stdio.h>

int main() 
{
    int base = 25;
    int height = 10;
    double area = 0.5 * base * height;

    printf("\ngiven base is: %d\n", base);
    printf("given height is: %d\n", height);
    printf("Area: %.2f\n", area);

    return 0;
}
